import readable

# This file is where all the gui will be written. For now, it will serve as the file to run for testing.
  
np.set_printoptions(threshold=sys.maxsize, suppress=True)

print(readable.start_new_map('cycle hit.osu', 0, 0, 0, 0, 1))
